export default {

  users(state) {
    return state.users;
  },

  infoMessage(state) {
    return state.infoMessage;
  },

  loader(state) {
    return state.loader;
  },

  form(state) {
    return state.form;
  },

  tasks(state) {
    return state.tasks;
  },

  taskUsername(state) {
    return state.taskUsername;
  },

  todo(state) {
    return state.todo;
  },

  done(state) {
    return state.done;
  }
}
